# Nginx reverse proxy produksi Quis (lihat docker-compose.prod.yaml).
# - :80 diredirect ke :443 (HTTPS).
# - :443 terminasi SSL, lalu:
#     /socket.io/ → realtime:4000  (WebSocket upgrade + read timeout 24j)
#     /           → app:3000
#
# Sertifikat: Letakkan fullchain.pem + privkey.pem di ./nginx/certs/
# (mis. dari Certbot/Let's Encrypt — lihat documentation/DEPLOYMENT.md).

# Map koneksi header untuk upgrade WebSocket.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    listen [::]:80;
    server_name _;
    # Redirect semua ke HTTPS.
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    listen [::]:443 ssl;
    server_name _;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1h;

    # Header keamanan dasar.
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Upload gambar: batasi ukuran sesuai MAX_UPLOAD_MB (5 MB default + margin).
    client_max_body_size 8m;

    # ---- Realtime Socket.IO (WAJIB upgrade WebSocket) ----
    # Tanpa blok ini, koneksi socket.io gagal saat polling→ws upgrade.
    location /socket.io/ {
        proxy_pass http://realtime:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # Tahan koneksi lama untuk game yang berjalan.
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ---- Next.js app ----
    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}
